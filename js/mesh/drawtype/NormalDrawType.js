
export default class {

    init(worldmap, matmgr) {
        this.worldmap = worldmap
        this.matmgr = matmgr
    }

    render(ctx, pos, node) {
        for (let side=0; side<6; side++){
            //TODO
        }
    }

}